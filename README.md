# 蜉蝣基金 (FundVal)

当前版本：`10.0.1`。

## V10 架构

- 页面先读本地缓存，再按基金逐只刷新；单只失败不会阻塞或清空其他基金。
- 刷新使用交易时段感知的递归定时器，页面隐藏时暂停网络刷新。
- 海外估值模型位于 `data/overseas-models.json`，结果保留模型版本、置信度和准确度台账。
- 展示来源统一为：`估`（盘中估值）、`净`（官方净值）、`模`（海外模型）、`旧`（旧缓存）。
- Gist 使用 Schema 2；兼容旧数组数据，覆盖和导入前自动生成本地备份。
- `sw.js` 按资源类型采用 network-first、cache-first 和 stale-while-revalidate。
- 使用 ES Modules 和 Node 内置测试，无第三方运行时依赖。

运行检查：

```bash
npm test
node --check js/app.js
```

个人基金盘中估值监控 PWA，托管于 [aureliuswu.github.io/FundVal](https://aureliuswu.github.io/FundVal/)。

## 定位

蜉蝣基金是一个轻量、纯前端、移动端优先的基金盘中观察工具。它不做复杂投研或自动交易，只帮助用户快速查看自选基金的估值、净值涨跌、持仓市值和主要市场行情。

## 功能

- 基金盘中估值、最新净值涨跌、持仓市值、今日盈亏。
- QDII/海外基金：优先展示最新公布净值涨跌，海外模型仅作为下一净值估算辅助。
- 顶部指数/黄金行情条。
- 多维排序：估值/净值涨跌、今日盈亏、市值、累计收益。
- 基金卡片展开：十大重仓股、基金信息、费率。
- 0 份额关注模式。
- GitHub Gist 云同步、JSON 导入导出。
- PWA 安装、离线缓存、更新提示、14:30 本地通知。

## 技术

零框架单页应用，无构建流程：

- `index.html`：页面结构与 PWA 入口。
- `js/app.js`：全部业务逻辑。
- `css/style.css`：样式与响应式布局。
- `manifest.json`：PWA 名称、图标、启动配置。
- `sw.js`：Service Worker 缓存和通知。

## 数据源

- 天天基金 JSONP：基金盘中估值。
- 东方财富 push2：备选净值、净值日涨跌幅、涨跌额。
- 东方财富 pingzhongdata：基金历史净值趋势，用于最新净值涨跌口径。
- 东方财富 fundf10：十大重仓、基金信息、费率。
- 腾讯行情：指数与海外模型成分行情。

## 开发

本项目没有 npm 依赖。常用检查：

```bash
node --check js/app.js
```

推送 `main` 分支后由 GitHub Pages 发布。

## 项目关系

- `FundVal`：蜉蝣基金，本项目。
- `pan`：盘中宝，独立维护的移动端盘中估值工具。
- `fund-compass`：司南基金，选基、择时、资产分析与回测系统。

## 风险提示

所有估值、模型、涨跌幅和通知仅供个人参考，不构成投资建议。
